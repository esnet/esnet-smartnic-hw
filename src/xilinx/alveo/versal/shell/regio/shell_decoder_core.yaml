name: bar2

decoders:
  hw_decoder: &hw_decoder
    # Placeholder — no Versal hw registers yet.
    # Replace with a Versal-specific hw decoder YAML as hardware registers
    # are added (e.g. CPM5, DCMAC, etc.).
    name: versal_hw
    interfaces: []
  core_decoder: &core_decoder
    !include core_decoder-ir.yaml

interfaces:
  - decoder: *hw_decoder
    address: 0x000000
    width: 22
    name: hw
  - decoder: *core_decoder
    address: 0x400000
    width: 22
    name: core
