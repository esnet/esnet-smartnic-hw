name: smartnic_app

blocks:
  empty: &empty
    name: empty
    info: |
      No custom registers included in p4-only flow.
    regs: []

interfaces:
  - block: *empty
    address: 0x00000
    width: 18
    name: empty
